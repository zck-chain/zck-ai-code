spring:
  application:
    name: zck-ai-code-mother
  # mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zck_ai_code
    username: root
    password: zck1125
server:
  port: 8123
  servlet:
    context-path: /api
springdoc:
  group-configs:
    - group: 'default'
      paths-to-match: '/**'
      packages-to-scan: com.zck.aicodemother.controller
knife4j:
  enable: true
  setting:
    language: zh_cn

